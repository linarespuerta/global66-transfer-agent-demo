# Copy this file to ".env" and paste your key. Never commit the real .env.
# Get a free key from Google AI Studio: https://aistudio.google.com/apikey
GOOGLE_API_KEY=AIza...

# Optional overrides (defaults are fine)
# GATHER_MODEL=gemini-2.5-flash   # cheap/fast model for Stage 1 (tool gathering)
# DECIDE_MODEL=gemini-2.5-flash   # Stage 2. NOTE: gemini-2.5-pro needs billing enabled
#                                 # (free tier quota = 0); use it here only if you've upgraded.
